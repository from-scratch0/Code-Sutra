{
  "name": "staticserver",
  "version": "1.0.0",
  "description": "可以在任意目录下启动一个静态文件服务器，并且把当前目录 做为静态文件根目录\r ```\r zf-server -d 指定静态文件根目录 -p 指定端口号 -o 指定监听的主机 \r ```\r ### node i -y",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "staticserver": "bin/www"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "debug": "^4.3.1",
    "handlebars": "^4.7.6",
    "yargs": "^16.2.0"
  }
}
